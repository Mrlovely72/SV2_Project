import { KHRCurrencyPipe } from './khr-currency-pipe';

describe('KHRCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new KHRCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
